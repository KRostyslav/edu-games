/** Статті довідника прологу. */

export const CODEX = {
  "request-lifecycle": {
    title: "Шлях запиту: від fetch() до бази й назад",
    aliases: ["what happens when you type url", "життєвий цикл запиту", "TTFB", "request lifecycle"],
    summary:
      "DNS → TCP → TLS → HTTP → балансувальник → процес застосунку → middleware і хендлер → база → JSON назад. Кожна ланка — окреме місце для затримки чи збою.",
    how:
      "Браузер спершу з'ясовує IP-адресу домену через DNS (відповідь кешується на TTL). Потім відкриває TCP-з'єднання — три пакети рукостискання — і поверх нього TLS: сервер показує сертифікат, сторони домовляються про ключі. Лише тоді йде HTTP-запит. Завдяки keep-alive наступні запити до того самого домену перевикористовують з'єднання й пропускають ці кроки.\n\n" +
      "На боці сервера запит зазвичай першим приймає балансувальник (nginx, ALB, Cloudflare): він завершує TLS і обирає один з інстансів застосунку. У процесі Node запит проходить роутер і ланцюжок middleware (логування, сесія, CORS, парсинг тіла) і потрапляє в хендлер. Хендлер бере з'єднання з пулу, робить запит до бази, серіалізує результат у JSON і відповідає.\n\n" +
      "Час, який браузер показує як Waiting (TTFB), — це все, що сталося на сервері: черга в балансувальнику, event loop процесу, очікування вільного з'єднання в пулі, сам SQL-запит і серіалізація.",
    code: [
      {
        lang: "text",
        caption: "curl -v показує ці кроки без браузера",
        src: [
          "*   Trying 203.0.113.10:443...",
          "* Connected to api.shop.ua (203.0.113.10) port 443",
          "* SSL connection using TLSv1.3 / TLS_AES_128_GCM_SHA256",
          "> GET /api/orders HTTP/2",
          "> host: api.shop.ua",
          "> cookie: sid=…",
          "< HTTP/2 200",
          "< content-type: application/json; charset=utf-8",
          '[{"id":1,"total":420}]',
        ].join("\n"),
      },
    ],
    tradeoffs: [
      "+ Keep-alive і HTTP/2 прибирають повторні рукостискання — другий запит у рази дешевший за перший.",
      "− Кожна мережева ланка додає свій таймаут і свою точку відмови; їх треба узгоджувати між собою.",
    ],
    numbers: [
      "DNS-запит без кешу: 20–100 мс.",
      "TCP + TLS 1.3 до сервера за океаном: ~2 RTT ≈ 150–250 мс.",
      "Простий індексований SQL-запит у тому ж дата-центрі: 1–5 мс.",
    ],
    interview: ["Що відбувається, коли ви вводите URL у браузер? Доведіть відповідь до бази даних і назад."],
    frontendBridge:
      "DevTools → Network → Timing: DNS Lookup, Initial connection, SSL, Waiting (TTFB), Content Download. Тепер ви знаєте, що ховається за Waiting.",
    see: ["http-handler", "api-errors"],
  },

  "http-handler": {
    title: "Хендлер у Node: req, res і контракт відповіді",
    aliases: ["node:http", "handler", "ендпоінт", "createServer", "request response"],
    summary:
      "Хендлер отримує запит (метод, URL, заголовки, тіло) і зобов'язаний завершити відповідь: статус, заголовки, тіло, res.end().",
    how:
      "Під Express, Fastify і NestJS лежить той самий `node:http`: функція `(req, res) => {}`, яку Node викликає на кожен запит. `req.method` і `req.url` (шлях разом із query-рядком) описують, чого хоче клієнт; `req.headers` — заголовки в нижньому регістрі. Відповідь збирається через `res.statusCode`, `res.setHeader()` і завершується `res.end(body)`.\n\n" +
      "Головне правило: кожен запит мусить отримати відповідь рівно один раз. Забутий `res.end()` — це запит, що висить до таймауту клієнта; подвійний — помилка ERR_HTTP_HEADERS_SENT. Заголовки відправляються разом із першим байтом тіла, тож змінити їх після `res.write()` уже не можна.\n\n" +
      "Хороший хендлер спершу відсікає некоректні запити (метод, формат параметрів) — дешево й без походу в базу, — і лише потім виконує роботу. А у відповідь явно збирає потрібні поля, а не віддає об'єкт з бази як є.",
    code: [
      {
        lang: "js",
        caption: "Мінімальний сервер без фреймворку",
        src: [
          'import { createServer } from "node:http";',
          "",
          "createServer((req, res) => {",
          '  const { pathname } = new URL(req.url, "http://localhost");',
          '  if (pathname === "/health") {',
          '    res.writeHead(200, { "Content-Type": "application/json" });',
          '    return res.end(JSON.stringify({ ok: true }));',
          "  }",
          "  res.statusCode = 404;",
          '  res.end("Not found");',
          "}).listen(3000);",
        ].join("\n"),
      },
    ],
    tradeoffs: [
      "+ Розуміння «голого» node:http робить будь-який фреймворк прозорим.",
      "− Без фреймворку легко забути про ліміт тіла, таймаути й обробку помилок — їх дають готові рішення.",
    ],
    interview: ["Що станеться, якщо хендлер не викличе res.end()? А якщо викличе двічі?"],
    frontendBridge:
      "Хендлер для бекенду — те саме, що компонент для React: функція, яку викликає рантайм. Тільки замість JSX вона повертає статус, заголовки й тіло — і не може «перерендеритись»: відповідь відправляється один раз.",
    see: ["api-errors", "request-lifecycle"],
  },

  "api-errors": {
    title: "Статуси й формат помилок API",
    aliases: ["status codes", "статус-коди", "4xx", "5xx", "problem details", "RFC 9457"],
    summary:
      "Статус — машинний сигнал для клієнта, кешів і моніторингу; тіло — людське пояснення. Помилка зі статусом 200 ламає і те, і інше.",
    how:
      "2xx — успіх (200 OK, 201 Created з заголовком Location, 204 No Content). 4xx — клієнт може виправити запит: 400 — некоректний формат, 401 — не автентифікований, 403 — автентифікований, але не має права, 404 — ресурсу немає, 409 — конфлікт стану, 422 — формат правильний, але дані не проходять бізнес-валідацію, 429 — забагато запитів. 5xx — проблема на сервері: 500 — непередбачена помилка, 502/504 — проблема з upstream, 503 — сервіс тимчасово недоступний.\n\n" +
      "Від статусу залежить поведінка всього довкола: fetch виставляє `res.ok`, клієнт вирішує, чи повторювати запит (5xx і 429 — можна, 4xx — зазвичай ні), CDN вирішує, чи кешувати, а моніторинг рахує error rate саме за 5xx.\n\n" +
      "Формат тіла помилки варто зробити єдиним для всього API — наприклад, `application/problem+json` з RFC 9457: `{ type, title, status, detail }`. Stack trace, SQL і внутрішні шляхи — лише в лог із request id, який можна показати користувачеві.",
    tradeoffs: [
      "+ Правильні статуси дають безкоштовні ретраї, кешування й алерти.",
      "− 404 проти 403 для чужого ресурсу — компроміс: 403 підтверджує, що ресурс існує.",
    ],
    interview: [
      "Чим 401 відрізняється від 403? А 400 від 422?",
      "Які помилки клієнт має право повторювати автоматично?",
    ],
    frontendBridge:
      "Той самий `if (!res.ok)` у вашому коді тепер має зміст: це ви вирішуєте, які відповіді клієнт вважатиме помилкою і чи покаже він «Спробуйте ще раз».",
    see: ["http-handler", "postmortem"],
  },
};
