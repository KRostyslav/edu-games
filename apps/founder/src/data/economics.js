/**
 * Економічні константи. Винесені окремо й названі, бо це єдине місце в грі,
 * де числа беруться з реального світу, а не з балансу.
 *
 * УВАГА: ставки й тарифи змінюються. Числа нижче — станом на кінець 2026 року
 * і потрібні грі для порядку величин, а не як податкова консультація.
 * Перед тим як спиратися на них у житті, перевірте актуальні.
 */

/**
 * Приймання оплат.
 *
 * Українському ФОП практично недоступний прямий Stripe, тому реалістичний
 * варіант — merchant of record (Paddle, Lemon Squeezy, FastSpring): вони самі
 * стають продавцем, беруть на себе податок з продажу в кожній країні й платять
 * вам як підряднику. Платите за це помітно більше, ніж «2.9% + $0.30»,
 * які всі цитують із документації Stripe.
 */
export const PROCESSOR = {
  label: "Merchant of record (Paddle / Lemon Squeezy)",
  /** Відсоток від обороту. */
  rate: 0.05,
  /** Фіксовано за кожну транзакцію, $. */
  perTransaction: 0.5,
};

/**
 * Податки ФОП 3-ї групи (неплатник ПДВ): 5% від обороту плюс ЄСВ.
 *
 * ЄСВ під час воєнного стану кілька разів змінювався й частково скасовувався,
 * тому тримаємо його окремою константою: підставте актуальну суму, і вся
 * фінансова модель перерахується.
 */
export const TAX = {
  label: "ФОП 3 група, 5% від обороту",
  rate: 0.05,
  /** ЄСВ на місяць, $ (умовний еквівалент — див. коментар вище). */
  socialFixed: 45,
};

/**
 * Інфраструктура. Базова вартість — хостинг, домен, пошта; далі росте з
 * кількістю клієнтів. Технічний борг робить те саме навантаження дорожчим:
 * неоптимізовані запити оплачуються рахунком за базу.
 */
export const INFRA = {
  base: 12,
  perCustomer: 0.55,
  debtPenalty: 150,
};

/**
 * Скільки годин на місяць з'їдає один клієнт підтримкою.
 *
 * Найоптимістичніше число в моделі й перше, що варто крутити при балансуванні.
 * Реально коливається в рази залежно від складності продукту й того, наскільки
 * добре написана документація.
 */
export const SUPPORT_HOURS_PER_CUSTOMER = 0.4;

/** Частка бюджету годин, більше якої підтримка не з'їдає — решта йде в борг тікетів. */
export const SUPPORT_MAX_SHARE = 0.5;

/**
 * Орієнтири, за якими гра оцінює здоров'я бізнесу. Це загальновживані
 * галузеві правила великого пальця, а не закони природи.
 */
export const BENCHMARKS = {
  /** Місячний відтік клієнтів для self-serve SMB SaaS. */
  churnGood: 3,
  churnOk: 7,
  churnBad: 10,
  /** LTV:CAC — нижче 3 юніт-економіка не сходиться. */
  ltvCacTarget: 3,
  /** Окупність залучення клієнта, місяців. */
  paybackTarget: 12,
  /** Вище цього відтоку формула LTV = ARPU / churn перестає бути правдою. */
  ltvUnreliableChurn: 10,
};

/** Множник до ARR у пропозиціях про купівлю проєкту. */
export const ACQUISITION_ARR_MULTIPLE = 2.3;
