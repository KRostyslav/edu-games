/**
 * Спільні механіки навчання: те, що не є ні рушієм симуляції (`sim-core`),
 * ні малюванням (`pixel-ui`), але потрібне кільком іграм однаково.
 */
export * from "./leitner.js";
export { dayNumber } from "./day.js";
